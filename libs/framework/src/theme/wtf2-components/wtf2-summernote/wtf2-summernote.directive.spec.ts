import { Wtf2SummernoteDirective } from './wtf2-summernote.directive';

describe('Wtf2SummernoteDirective', () => {
  it('should create an instance', () => {
    const directive = new Wtf2SummernoteDirective();
    expect(directive).toBeTruthy();
  });
});
