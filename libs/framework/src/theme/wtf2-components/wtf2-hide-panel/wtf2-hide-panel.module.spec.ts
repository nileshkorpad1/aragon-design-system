import { Wtf2HideButtonModule } from './wtf2-hide-button.module';

describe('Wtf2HideButtonModule', () => {
  let wtf2HideButtonModule: Wtf2HideButtonModule;

  beforeEach(() => {
    wtf2HideButtonModule = new Wtf2HideButtonModule();
  });

  it('should create an instance', () => {
    expect(wtf2HideButtonModule).toBeTruthy();
  });
});
