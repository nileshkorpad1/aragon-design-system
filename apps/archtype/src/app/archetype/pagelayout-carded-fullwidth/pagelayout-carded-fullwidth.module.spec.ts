import { PagelayoutCardedFullwidthModule } from './pagelayout-carded-fullwidth.module';

describe('PagelayoutCardedFullwidthModule', () => {
  let pagelayoutCardedFullwidthModule: PagelayoutCardedFullwidthModule;

  beforeEach(() => {
    pagelayoutCardedFullwidthModule = new PagelayoutCardedFullwidthModule();
  });

  it('should create an instance', () => {
    expect(pagelayoutCardedFullwidthModule).toBeTruthy();
  });
});
